<div class="card">
    <div class="card-body">
        <h5 class="card-title">
            {{recipe.title}}
            <div class="card-title">
                {{recipe.typeRecipe}}
            </div>
        </h5>
        <p class="text text-muted">
            {{recipe.description}}
        </p>
    </div>
</div>